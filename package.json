{
  "name": "starhold",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "wasm": "bash scripts/build-wasm.sh",
    "dev": "npm run wasm && vite --port 5199 --strictPort",
    "build": "npm run wasm && tsc --noEmit && vite build",
    "preview": "vite preview --port 5199 --strictPort",
    "capture": "node scripts/capture.mjs",
    "test:menu:browser": "node scripts/menu-browser.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.6"
  }
}
