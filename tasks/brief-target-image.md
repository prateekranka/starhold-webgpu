Use your image generation tool to create the target screenshot described in the attached prompt file.

Read `/home/bobbyranka/Cowork/starhold/.dream-loop/target-prompt.md` and generate ONE image from it. Save the result to `/home/bobbyranka/Cowork/starhold/.dream-loop/target.png`.

Requirements:
- Generate exactly one image. Do not iterate on it or generate variants.
- Save the full-resolution PNG exactly as generated (do not resize).
- Reply with the absolute path and the pixel dimensions.

Note: the prompt describes a 480x270 pixel-art game screenshot. Ask the image model for that exact composition; if the model can only produce larger sizes, keep the pixel-art look (hard pixel edges, exact 32-color palette, no antialiasing).